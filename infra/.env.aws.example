# AWS creds for the deployer to run infra + provision agents.
# Copy to infra/.env.aws (gitignored) and fill in. NEVER commit the real file.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
