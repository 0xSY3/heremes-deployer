# Supabase — the agent store (Postgres). Copy to infra/.env.supabase (gitignored).
# ROTATE the service key first if it was exposed.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_KEY=
